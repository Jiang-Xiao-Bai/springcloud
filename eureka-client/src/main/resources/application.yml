spring:
  application:
    name: eureka-client

server:
  port: 8080

#配置注册中心
eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8761/eureka/
  instance:
    prefer-ip-address: true
